pandas>=2.2
numpy>=1.26
requests>=2.31
python-dotenv>=1.0
tqdm>=4.66
scikit-learn>=1.5
matplotlib>=3.9

# GPU strongly recommended for analyze_reviews.py (RoBERTa-large over ~11k
# reviews). Install torch separately per https://pytorch.org/get-started/locally/
# for the right CUDA build - the plain "pip install torch" below only gets
# you a CPU build.
torch>=2.2
transformers>=4.40
